# semgrep-rules-cloud
Semgrep rules but for cloud anti-patterns rather than security. Detect statefulness, poor configuration management and secrets handling, and other markers which indicate cloud native suitability.
